import BookmarksComp from "./BookmarksComp";

export const metadata = {
  title: "Bookmarks",
  description: "Saved articles for later reading",
};

const Bookmarks = (): React.JSX.Element => {
  return <BookmarksComp />;
};

export default Bookmarks;
